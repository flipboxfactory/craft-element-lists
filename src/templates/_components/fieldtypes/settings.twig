{% import "_includes/forms" as forms %}
{% extends "_components/fieldtypes/elementfieldsettings" %}

{% block advancedSettings %}
    {{ forms.lightswitchField({
        label: "Sortable"|t('element-lists'),
        instructions: "Whether the field relations can be sorted."|t('element-lists'),
        id: 'sortable',
        name: 'sortable',
        enabled: field.sortable ? true : false,
        errors: field.getErrors('sortable')
    }) }}

    {{ parent() }}
{% endblock %}